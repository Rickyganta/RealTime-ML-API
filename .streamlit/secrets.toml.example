# Streamlit Community Cloud: Settings → Secrets (TOML). Local: copy to secrets.toml (gitignored).
# HTTPS origin only, no trailing slash (Streamlit Cloud cannot reach localhost).
API_BASE = "https://YOUR_API_HOST.up.railway.app"
