# Streamlit Community Cloud: paste into Settings → Secrets (TOML).
# Local: copy to `secrets.toml` in this folder (see .gitignore — do not commit real secrets).
#
# Must be a URL your Streamlit app can reach over the internet (not localhost from Cloud).
API_BASE = "https://your-public-fastapi-host.example"
