# Monitoring Dashboard Screenshots

Add screenshots here after running the stack:

1. Streamlit dashboard home (`http://localhost:8501`)
2. Prometheus metrics sample (`/metrics`)
3. Locust load test dashboard

Suggested filenames:

- `dashboard-overview.png`
- `metrics-endpoint.png`
- `locust-latency.png`
